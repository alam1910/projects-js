In JavaScript, **reserved words** (also called keywords) are words that have special meaning in the language and **cannot be used as variable, function, or class names**.

Here are the main JavaScript reserved words (ES6+):

### Control Flow

```
if
else
switch
case
default
for
while
do
break
continue
return
throw
try
catch
finally
```

### Declarations

```
var
let
const
function
class
extends
import
export
default
```

### Object / Context

```
this
super
new
delete
typeof
instanceof
in
void
with
```

### Async / Advanced

```
async
await
yield
```

### Error / Debug

```
debugger
```

### Other Reserved Keywords

```
enum
implements
interface
package
private
protected
public
static
```

> ⚠️ Note: Some words like `enum`, `implements`, `interface`, etc., are reserved for future use or in strict mode.

--